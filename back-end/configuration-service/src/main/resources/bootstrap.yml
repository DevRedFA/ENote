server:
  port: 8888

spring:
  application:
    name: configuration-service
  profiles:
    active: native
  cloud:
    config:
      server:
        bootstrap: true
        native:
          search-locations: classpath:configurations/
      fail-fast: true
